@model List<string>

<h1>@ViewBag.Title</h1>
<p>@ViewBag.Message</p>

@for(int i = 0; i < 10; i++)
{
     <p>The current value of i is @i</p>
}

<select>
    @foreach(var city in Model)
    {
         <option value="@city">@city</option>
    }
</select>


